package groupe7.indicateurs.infrastructure.personne.service;

import org.springframework.stereotype.Service;

import groupe7.indicateurs.domain.personne.model.Personne;


@Service
public class PersonneInfrastructureService {
    // Méthodes pour interagir avec la base de données ou d'autres services externes
    public Personne findPersonneById(Long id) {
        // Logique pour trouver un personnel par ID (par exemple via un repository)
        return null;
    }
}
